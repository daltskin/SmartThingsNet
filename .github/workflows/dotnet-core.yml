name: .NET Core

on:
  push:
    branches: [ master ]
    paths-ignore: 
        - 'docs/**'
        - '**.md'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
      
    - name: Checkout
      uses: actions/checkout@v2
    - name: Fetch all history for all tags and branches
      run: git fetch --prune --unshallow   
  
    - name: Install GitVersion
      uses: gittools/actions/gitversion/setup@v0.9.7
      with:
          versionSpec: '5.5.0'
    
    - name: Get SemVer
      uses: gittools/actions/gitversion/execute@v0.9.7
       
    - name: Display SemVer $GITVERSION_SEMVER
      run: |
       echo "SemVer: $GITVERSION_SEMVER"

    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.*   
    
    - name: Install dependencies
      run: dotnet restore
    - name: Build ${{ env.GitVersion_SemVer }} Docker ${{ env.GITVERSION_SEMVER }} Lower ${{ env.GITVERSION_semVer }}
      run: dotnet build --configuration Release --no-restore /p:AssemblyVersion=${{ env.GitVersion_SemVer }} /p:FileVersion=${{ env.GitVersion_AssemblySemFileVer }} /p:InformationalVersion=${{ env.GitVersion_InformationalVersion  }}
    - name: Test
      run: dotnet test --no-restore --verbosity normal

#    - name: Setup nuget config
#      run: sed -i 's/GITHUB_TOKEN/${{ secrets.GITHUB_TOKEN }}/g' nuget.config
#      working-directory: src

    - name: Package
      run: dotnet pack --configuration Release --no-build -p:PackageVersion=${{ env.GitVersion_SemVer}}
    
#    - name: Publish to github
#      run: dotnet nuget push **/*.nupkg -s https://nuget.pkg.github.com/daltskin/index.json -k ${{ secrets.GITHUB_TOKEN }} --skip-duplicate --no-symbols true
#      working-directory: src
 
    - name: Publish to github (using curl)
      run: curl -vX PUT -u "daltskin:${{ secrets.GITHUB_TOKEN }}" -F package=@SmartThingsNet.${{ env.GitVersion_SemVer}}.nupkg https://nuget.pkg.github.com/daltskin/
      working-directory: src/bin/Release
 
    - name: Publish to nuget
      run: dotnet nuget push **/*.nupkg -s https://api.nuget.org/v3/index.json -k ${{secrets.NUGET_API_KEY}} --skip-duplicate
      
    - name: Create Release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ env.GitVersion_SemVer}}
        release_name: Release ${{ env.GitVersion_SemVer}}
